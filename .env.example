# Copy this file to `.env` and fill in secrets used locally (DO NOT commit `.env`)
# Set your GROQ API key here for local development
GROQ_API_KEY=REPLACE_ME
# GROQ_API_KEY sample (redacted) — do NOT paste real keys here
# Optional: override the GROQ API URL and model
# GROQ_API_URL=https://api.groq.ai/v1/generate
# GROQ_MODEL=groq-1

# Optional: image registry credentials for CI/CD (store these as repository secrets)
DOCKERHUB_USERNAME=DOCKERHUB_USERNAME_PLACEHOLDER
DOCKERHUB_TOKEN=REPLACE_ME
